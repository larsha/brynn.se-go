version: '2'
services:
  test:
    extends:
      file: docker-compose.base.yml
      service: base
    command: go test -v
  web:
    extends:
      file: docker-compose.base.yml
      service: base
